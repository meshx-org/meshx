{
    "name": "src/typescript/fiber-kernel",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "src/typescript/fiber-kernel/src",
    "projectType": "library",
    "targets": {
        "build": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/./src/typescript/fiber-kernel",
                "main": "src/typescript/fiber-kernel/src/index.ts",
                "tsConfig": "src/typescript/fiber-kernel/tsconfig.lib.json",
                "assets": ["src/typescript/fiber-kernel/*.md"],
                "updateBuildableProjectDepsInPackageJson": true
            }
        },
        "publish": {
            "executor": "nx:run-commands",
            "options": {
                "command": "node tools/scripts/publish.mjs src-typescript-kernel {args.ver} {args.tag}"
            },
            "dependsOn": ["build"]
        },
        "lint": {
            "executor": "@nx/linter:eslint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["src/typescript/fiber-kernel/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nx/vite:test",
            "outputs": ["{workspaceRoot}/coverage/src/typescript/kernel"],
            "options": {
                "passWithNoTests": true,
                "reportsDirectory": "../../../coverage/src/typescript/kernel"
            }
        }
    },
    "tags": ["lang:ts"]
}
